incdir = include_directories('../')

executable('trainer',
           'main.cpp',
           'trainer.cpp',
            include_directories : incdir,
            dependencies : [ instrument_dep, common_dep ],
            install : false,
          )